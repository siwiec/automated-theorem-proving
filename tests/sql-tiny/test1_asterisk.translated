% Equivallence check for file tests/sql-tiny/test1_asterisk.sql...
% CREATE TABLE tab (
%   fst NUMBER(4),
%   snd NUMBER(4),
%   trd NUMBER(4),
%   CONSTRAINT tab_pk PRIMARY KEY (fst, snd)
% );
% 
% SELECT a.*
% FROM   tab a,
%        tab b
% WHERE  ( a.fst = b.snd
%          AND b.fst = a.snd
%          AND a.snd < b.snd );

% Database scheme:
% tab:
%	fst: Number
%	snd: Number
%	trd: Number

% Translating query 0...
% SelectStatement
%   Select
%     { qeSetQuantifier = SQDefault
%     , qeSelectList =
%         [ ( BinOp (Iden [ Name Nothing "a" ]) [ Name Nothing "." ] Star
%           , Nothing
%           )
%         ]
%     , qeFrom =
%         [ TRAlias
%             (TRSimple [ Name Nothing "tab" ])
%             (Alias (Name Nothing "a") Nothing)
%         , TRAlias
%             (TRSimple [ Name Nothing "tab" ])
%             (Alias (Name Nothing "b") Nothing)
%         ]
%     , qeWhere =
%         Just
%           (Parens
%              (BinOp
%                 (BinOp
%                    (BinOp
%                       (Iden [ Name Nothing "a" , Name Nothing "fst" ])
%                       [ Name Nothing "=" ]
%                       (Iden [ Name Nothing "b" , Name Nothing "snd" ]))
%                    [ Name Nothing "and" ]
%                    (BinOp
%                       (Iden [ Name Nothing "b" , Name Nothing "fst" ])
%                       [ Name Nothing "=" ]
%                       (Iden [ Name Nothing "a" , Name Nothing "snd" ])))
%                 [ Name Nothing "and" ]
%                 (BinOp
%                    (Iden [ Name Nothing "a" , Name Nothing "snd" ])
%                    [ Name Nothing "<" ]
%                    (Iden [ Name Nothing "b" , Name Nothing "snd" ]))))
%     , qeGroupBy = []
%     , qeHaving = Nothing
%     , qeOrderBy = []
%     , qeOffset = Nothing
%     , qeFetchFirst = Nothing
%     }

